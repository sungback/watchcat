# Tests

Run the current test suite with:

```bash
python3 -m unittest discover -s tests -v
```

The suite currently focuses on pure file-normalization behavior that can run
without a Flet UI harness.

